name: Lean CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lean-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.shards.outputs.matrix }}

    steps:
      - name: Check out the exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Test and run the comment-aware unfinished-proof check
        shell: bash
        run: |
          set -euo pipefail
          python3 -B -m unittest discover -s scripts/ci -p 'test_*.py'
          python3 -B scripts/ci/check_lean_proof_commands.py

      - name: Plan complete Lean source coverage
        id: shards
        shell: bash
        run: >-
          python3 -B scripts/ci/plan_lean_build_shards.py
          --production-shards 72 --test-shards 24
          --github-output "$GITHUB_OUTPUT"

  build-and-audit:
    needs: plan
    name: Build and audit ${{ matrix.shard.name }} (${{ matrix.shard.moduleCount }} modules)
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(needs.plan.outputs.matrix) }}
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      # Four workers are the previously verified bounded setting for hosted
      # complete builds.  Seventy-two production shards keep the enlarged
      # complete-source coverage below GitHub's six-hour job deadline without
      # returning to the unbounded concurrency that caused memory faults.
      LEAN_NUM_THREADS: '4'

    steps:
      - name: Check out the exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Build every module in this shard
        uses: leanprover/lean-action@38fbc41a8c28c4cbaec22d7f7de508ec2e7c0dd9 # v1.5.0
        with:
          auto-config: false
          build: true
          build-args: ${{ matrix.shard.buildModules }}
          use-mathlib-cache: true

      - name: Enforce transitive axiom bounds on production shards
        if: ${{ matrix.shard.axiomGate }}
        shell: pwsh
        env:
          SHARD_MODULES: ${{ matrix.shard.modules }}
        run: |
          $ErrorActionPreference = 'Stop'
          $arguments = @('scripts/ci/run_axiom_gate.py')
          foreach ($module in ($env:SHARD_MODULES -split ' ')) {
            if ($module) {
              $arguments += '--entry'
              $arguments += $module
            }
          }
          python3 -B @arguments
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }

      - name: Verify clean generated state
        shell: bash
        run: test -z "$(git status --porcelain)"

  axiom-gate-fixtures:
    needs: plan
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      LEAN_NUM_THREADS: '1'

    steps:
      - name: Check out the exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Negatively test the enforcing gate
        uses: leanprover/lean-action@38fbc41a8c28c4cbaec22d7f7de508ec2e7c0dd9 # v1.5.0
        with:
          auto-config: false
          build: true
          build-args: BongTest.AxiomGate
          use-mathlib-cache: true

      - name: Run actual positive and negative fixtures
        shell: bash
        run: python3 -B scripts/ci/check_axiom_gate_fixtures.py
