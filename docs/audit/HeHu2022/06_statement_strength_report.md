# Statement strength report

`heHu2022Theorem11` proves the exact biconditional target encoded by
`HeHuTheorem11Statement`, including all parity and ambient-rank branches.
No direction is left as a statement-only declaration.

`heHu2022Theorem12PublishedEvenLiteral` and
`heHu2022Theorem12PublishedOddLiteral` are stronger than the earlier abstract
maximal-testing reduction in exactly the ways required by the publisher
statement: their index is finite, every parameter is literally `delta` or
`delta*pi` for `delta` in `U`, the rank-two undefined row is removed, the
rows are proved irredundant, and minimality is formulated by deletion of an
individual member rather than only after quotienting duplicate
presentations.

The only nonliteral definitional choice is Definition 3.1: `heHuSharp` is a
canonical witness selected by the exact properties established in
Proposition 3.2. This is extensionally sufficient for every downstream use
but remains marked for independent semantic review.
