# Lemmas 6.9--6.11 checkpoint

Status: `FULLY_FORMALIZED` / semantic `PROVISIONAL_MATCH` for each of
Lemmas 6.9, 6.10, and 6.11.

Code checkpoint: `382ef7ab3014e08834342ae8b806b15b33aaabb8`.

## Source authority

The sole semantic authority is the publisher version of record:

- Zilong He, *On n-ADC integral quadratic lattices over algebraic number
  fields*, *Documenta Mathematica* 30 (2025), no. 4, 981--1022;
- DOI: <https://doi.org/10.4171/DM/1003>;
- publisher PDF SHA-256:
  `E26190C88B16624DCCB7F269C6C3FFDA02BC6830677A5BC0C8E0AD48A36E72D6`.

The statements and proof routes were checked against the printed Lemmas
6.9--6.11. The post-publication arXiv revision remains comparison material
only.

## Lemma 6.9

`GoodBONG.heADC2025Lemma69` has the printed hypotheses:

- the first three orders are `0, -2e, 0`;
- the ambient space is `W_1^4(Delta)`;
- `kappa` is a unit with quadratic defect `2e-1`;
- the lattice represents both `N_1^2(kappa)` and `N_2^2(kappa)`.

Its conclusion is the exact alternative `R_4 = -2e` or `R_4 = 2-2e`.
The proof uses the literal condition-(iii) trigger in the formalized
Theorem 3.6. If `R_4 > 2-2e`, both binary test spaces would be represented
by the first three source coordinates, contradicting Lemma 4.5(i). The
general adjacent-order lower bound and Corollary 3.2(i) then exclude the
only intervening integral value. The full ambient defect and both target
defects are derived rather than assumed as numerical proxies.

## Lemma 6.10

`GoodBONG.heADC2025Lemma610` starts with the exact exceptional order profile
`[0, -2e, 0, 2-2e]` in `W_1^4(Delta)` and concludes integral lattice
isometry with the constructed lattice

`H perp <1, -Delta * pi^(2-2e)>`.

The proof checks all four conditions of Beli's classification theorem used
in the paper. It proves the alpha sequence `(0, 2e, 1)`, all three prefix
defect bounds, and the only active internal prefix representation. The
source lattice's integrality follows from its first good-BONG order; it is
not an added premise. Thus the endpoint proves actual lattice isometry, not
only equality of orders or ambient quadratic-space isometry.

## Lemma 6.11

`Lattice.heADC2025Lemma611` is stated for an arbitrary quaternary 2-ADC
lattice in `W_1^4(Delta)`. Its conclusion is exactly the printed disjunction:
the maximal lattice `N_1^4(Delta)` or the exceptional lattice above.

The proof constructs all four binary tests from the 2-ADC hypothesis and the
ambient isometry:

- `N_1^2(1)` and `N_1^2(Delta)` give the first three orders through
  Lemma 6.4(ii);
- existence of a unit `kappa` of defect `2e-1` is proved from the dyadic
  context;
- both `N_1^2(kappa)` and `N_2^2(kappa)` are obtained by the codimension-two
  ambient embeddings and the definition of 2-ADC.

Lemma 6.9 supplies the terminal alternative. The `-2e` branch is identified
with `N_1^4(Delta)` by the fully proved Lemma 4.11(i) profile criterion, and
the `2-2e` branch is identified with the exceptional lattice by Lemma 6.10.
No good BONG, test representation, profile, or auxiliary classification law
appears in the public theorem's assumptions.

## Mechanical trust checks

The three new modules, canonical paper entry, and full paper audit entry
compile directly with Lean 4.32.1. Ten newly printed transitive dependency
sets contain exactly `propext`, `Classical.choice`, and `Quot.sound`. The
focused enforcing gate checked 57,918 declarations. The comment-aware source
scanner checked 2,708 tracked Lean sources and found no forbidden proof token
outside comments. The source files satisfy the 100-column project limit.

The package workflow for this exact checkpoint is
[run 33981160696](https://github.com/hzldew-git/BongTheory/actions/runs/33981160696);
its result remains a separate deployment gate until the run completes.

## Scope boundary

This checkpoint raises Section 6 to 9/12 fully matched numbered items. It
does not turn the false `n=2` instance of Lemma 6.8(iv) into a theorem, and
does not certify Theorem 6.2 or Remark 6.3. In particular, Lemma 6.11 concerns
the first discriminant ambient space and does not repair the distinct
second-column boundary mismatch documented in reports 30--31.

The semantic label remains `PROVISIONAL_MATCH` until human source and
formalization reviewers sign the result cards.

## Author review card

Reviewers should confirm:

1. the capped-defect trigger in Lemma 6.9 is the printed Theorem 3.6(iii)
   trigger, including the terminal index convention;
2. the four Beli classification conditions in Lemma 6.10 match the source
   normalization and sign convention for the exceptional lattice;
3. Lemma 6.11 derives, rather than assumes, all four maximal binary tests;
4. the two terminal-order branches are identified by the exact published
   Lemmas 4.11(i) and 6.10.

Author decision: unsigned. Domain-expert decision: unsigned.
Formalization-expert decision: unsigned.
