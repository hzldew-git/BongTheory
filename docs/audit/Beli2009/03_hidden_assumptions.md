# Hidden-assumption expansion

The ambient `DyadicContext` has the field/valuation meaning described in
`TRUST.md` and contains no classification conclusion. `GoodBONG` makes the
paper's chosen BONG and goodness conditions explicit. `ClassificationConditions`
bundles equal orders, equal alpha invariants, prefix defect bounds, and
internal representation conditions.

`HasResidueFieldMoreThanTwoElements` is the exact cardinality split used by
the final paper discussion. `Beli2009BinaryReachable` is reflexive-transitive
finite reachability under the formal one-step binary transformation, not an
unbounded external algorithm.

All core hidden-structure comparisons are `PROVISIONAL_MATCH`. No internal
`...Laws` or `...Data` package survives as an assumption of the public core
endpoints.
